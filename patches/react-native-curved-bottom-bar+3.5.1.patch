diff --git a/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/components/CurvedView/curvedViewExpo.tsx b/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/components/CurvedView/curvedViewExpo.tsx
index 3f04f79..dca59dd 100644
--- a/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/components/CurvedView/curvedViewExpo.tsx
+++ b/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/components/CurvedView/curvedViewExpo.tsx
@@ -2,6 +2,8 @@ import React from 'react';
 import { View } from 'react-native';
 import { Path, Svg } from 'react-native-svg';
 import { CurvedView } from './model';
+import DropShadow from "react-native-drop-shadow";
+
 
 const CurvedContainer: CurvedView = (props) => {
   const { style, width, height, bgColor, path, borderColor, borderWidth } =
@@ -14,9 +16,20 @@ const CurvedContainer: CurvedView = (props) => {
 
   return (
     <View style={style}>
-      <Svg width={width} height={height}>
-        <Path fill={bgColor} {...{ d: path }} {...border} />
-      </Svg>
+      <DropShadow style={{
+        shadowColor: "#DDDDDD",
+        shadowOffset: {
+          width: 0,
+          height: 0,
+        },
+        shadowOpacity: 1,
+        shadowRadius: 5,
+      }}
+      >
+        <Svg width={width} height={height}>
+          <Path fill={bgColor} {...{ d: path }} {...border} />
+        </Svg>
+      </DropShadow>
     </View>
   );
 };
diff --git a/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/utils/pathDown.ts b/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/utils/pathDown.ts
index e079714..9e41df0 100644
--- a/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/utils/pathDown.ts
+++ b/node_modules/react-native-curved-bottom-bar/src/CurvedBottomBar/utils/pathDown.ts
@@ -222,7 +222,7 @@ export const getPathDown = (
 
   return `${line(width, height, centerWidth)} ${lineCurved(
     width / 2 - circleWidth / 2,
-    height,
+    height - 25,
     circleWidth
   )}`;
 };
